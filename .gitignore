# --- Node / npm ---
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm/

# TypeScript build output (published to npm via `npm run build`, not committed)
dist/

# --- Android (Gradle) ---
android/build/
android/.gradle/
android/local.properties
android/*.iml
android/.idea/
android/captures/
android/.externalNativeBuild/
android/.cxx/

# --- iOS (Swift Package Manager) ---
ios/.build/
ios/.swiftpm/
ios/*.xcodeproj/xcuserdata/
ios/*.xcworkspace/xcuserdata/
# Library, not an app — resolved versions shouldn't be locked for downstream consumers.
ios/Package.resolved

# --- Editors / OS ---
.DS_Store
**/.DS_Store
.vscode/
.idea/
*.swp
